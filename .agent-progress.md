## Objective
- Hybrid EfficientNet-B2 + SVM solar-panel dust detection (clean/dirty) with 5 XAI methods, backbone comparison, README, and an IEEE paper fully reconciled to the delivered binary system.

## Important Details
- User: Aditya Shirsatrao, N K Orchid College of Engg & Tech, Solapur; single-author IEEE paper.
- Paper must present results as already achieved (no future tense); now a proper IEEEtran document.
- TensorFlow 2.19.0 + GPU via venv `/home/aditya/venv/bin/python`; system `python` lacks tf/flask.
- Port 5001 (5000 = MLflow). Flask dashboard on 5001 via systemd + Gunicorn, `Restart=always`.
- Dataset ACTUAL split (verified by counting files): train 674 (402 clean/272 dirty), val 84 (50/34), test 84 (50/34) = **842 total**.
- SVM head is trained on the **union of train+val (758)** and evaluated on the held-out test (84); 5-fold CV also on 758. (Metadata field `train_samples`=758 reflects this.)
- Frozen EfficientNet-B2 + RBF-SVM is the deployed model: **97.62% acc, 0.9971 AUC, 97.89% 5-fold CV**, 7.77M params, 1408-d features. MobileNetV3-Large + SVM = 98.81% (smallest, 3.00M). ResNet50 + SVM = 97.62% (23.59M).
- `Models/finetuned_backbone_weights.weights.h5` was orphaned (mismatched with frozen SVM) and DELETED.
- `~/Desktop/Downloads` is a broken symlink → `/mnt/ssd/cardiact/moved/Downloads` (does not exist). Use `~/Desktop/` for zips.
- CITATIONS FULLY VERIFIED (web-checked): SolNet (Energies 2023, 16(1):155, 98.2%), Alatwi (Sustainability 2024, 16(19):8664, DenseNet169+SVM 86.79%), Bassil (ISWA 2025, 26:200509, 97%), Kourtiche (IJEECS 2026, 41(1):409, survey). Fabricated `zhang2023fault` + `hesham2026early` REMOVED. All 16 cited refs are real with correct DOIs/venues.

## Work State
### Completed
- **Paper P0 reconciled + IEEE-formatted**: `paper/main.tex` rewritten to binary 97.62% system; converted from `article` → `IEEEtran` class (4-page clean PDF, 16 citations resolved, 0 errors, 0 undefined refs). Clarified SVM trains on train+val(758)/test(84).
- **Citations verified & corrected**: fixed onim/alatwi/bassil entries (author/venue/numbers); removed 2 fabricated; added real kourtiche2026 survey.
- **Comparison table fixed**: was misattributing SolNet→"VGG16+Softmax 91.2%" and Bassil→"ResNet50+SVM 93.8%"; now shows verified literature (SolNet 98.2%, Alatwi 86.79%, Bassil 97%) + our 3 backbones with cross-dataset caveat.
- **Tests green (9/9)**: `app.py` logging made fault-tolerant (no FileHandler crash when `logs/app.log` root-owned).
- **Orphaned weights removed**.
- **Security fix**: systemd service now runs as `aditya` (non-root). `gunicorn.conf.py` logs to `logs/gunicorn_*.log` (aditya-creatable) instead of root-owned files.
- **Code clean**: `explanations.py` default weights path → None (no dangling ref to deleted file); all modules `py_compile` OK.
- README accurate; figures (fig3/5/6/7/9/10) copied into `paper/figures/` as binary versions.
- Overleaf zip refreshed at `~/Desktop/solar_dust_paper_overleaf.zip`.

### Active
- (none)

### Blocked
- (none)

## Next Move
- No blocking work. Optional polish: add `flask-limiter` usage or remove it from requirements; collapse duplicate `model_versions.json` entries; surface the 0.85 confidence-flag in the dashboard UI (currently described in paper only).

## Relevant Files
- `paper/main.tex` — IEEEtran, binary 97.62% system, verified citations.
- `paper/references.bib` — 16 real entries (onim2023solnet, alatwi2024deep, bassil2025efficient, kourtiche2026, + foundational CNN/XAI refs).
- `paper/main.pdf` — compiled 4-page IEEE PDF.
- `~/Desktop/solar_dust_paper_overleaf.zip` — submission package.
- `app.py` — Flask dashboard (port 5001); fault-tolerant logging.
- `explanations.py` — 5 XAI methods; frozen weights only.
- `train_solar_dust.py` — backbone registry, `--backbone`, batched extraction, comparison CSV.
- `gunicorn.conf.py` — logs to `logs/gunicorn_*.log`.
- `/etc/systemd/system/solar-dust-detection.service` — `User=aditya`.
- `Models/pipeline_meta.json`, `Models/comparison_results.csv` — ground-truth metrics (note train_samples=758 = train+val).
- `data/{train,val,test}/{clean,dirty}/` — 674/84/84 = 842 images.
